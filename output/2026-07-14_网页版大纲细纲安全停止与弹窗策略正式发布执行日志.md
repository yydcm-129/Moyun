# 网页版大纲细纲安全停止与弹窗策略正式发布执行日志

> 日期：2026-07-14
> 发布范围：Pages 仓 `单文件版/output/deploy-staging/`。
> 授权：用户已明确要求“再次复查后上线，并做本次公告上线”。
> 冻结边界：不修改、不复制、不回填任何 `单文件版/` 离线宿主、白鸟 JSON、纯净版、交付包或离线测试资产。

## 本次发布内容

1. 总纲与细纲拥有各自的取消控制器与 runId；用户停止后不进入协议 fallback，不接收迟到结果。
2. 生成中的总纲/细纲关闭工作台前会确认“继续生成 / 停止并关闭”；细纲保留已完成批次。
3. 宿主弹窗按 viewer / explicit / locked 策略关闭，编辑草稿不会再因蒙版误触静默丢失。
4. 公告 ID 更新为本次安全停止与弹窗策略版本，刷新网页会显示新公告。

## 发布前基线

- 分支：`main`
- 起始 HEAD：`33c334bcb8baa5fb2c6b1cbd46fc62a27f4c4672`
- 已完成本地 Edge 双端回归：桌面 `1440x960`、手机 `390x844`，报告为 `output/playwright/outline_modal_safety_g4_report.json`，所有断言通过。
- 发布前只允许提交网页源码、构建产物和本日志；`.playwright-cli/`、其它 `output/` 证据文件与此前计划文档均不暂存。

## 阶段状态

- [x] R1：候选与公告复查
- [x] R2：公告更新与构建
- [x] R3：发布前桌面 / 手机验收
- [x] R4：提交、推送、Pages 线上验收

## R2：公告与构建

- 公告 ID：`web-2026-07-14-outline-safe-stop-and-modal-policy`。
- 公告标题：`大纲与弹窗安全优化`；正文说明继续生成、停止并关闭、未完成内容不写入、细纲完成批次保留及弹窗防误关。
- 已运行 `npm run build`，生成 `index.html` 与 `assets/js/moyun.js`；JS 版本 `96f3ee7af646`。
- 已运行 `node --check assets/js/moyun.js` 与 `git diff --check`，均通过。

## R3：发布前 Edge 双端验收

- 使用延迟 mock OpenAI-compatible API 重跑完整回归，报告：`output/playwright/outline_modal_safety_g4_report.json`，`passed=true`、页面错误 `0`、route handler 错误 `0`。
- 桌面 `1440x960`：总纲继续/停止、细纲第二批停止、公告文本、viewer/explicit/locked 关闭策略、连接中心模块专属路由均通过。
- 手机 `390x844`：公告中心点 `195px` 对齐视口中心 `195px`；连接编辑器无横向溢出（`scrollWidth=390`）；草稿关闭会显示放弃确认。
- 本轮公告截图：`output/playwright/g4-desktop-release-announcement.png`、`output/playwright/g4-mobile-release-announcement.png`。

## R4：正式发布与线上验收

- 功能发布提交：`87e115e feat: safeguard outline stop and modal dismissal`。
- 已推送：`origin/main`，范围 `33c334b..87e115e`。
- GitHub Pages 实页返回 `200`，页面引用 `./assets/js/moyun.js?v=96f3ee7af646`，与本地构建版本一致。
- 线上独立 Edge 冒烟：新公告 ID、标题、正文、桌面可见性、手机居中与无横向溢出全部通过；应用级 `pageerror=0`。
- 线上报告：`output/playwright/online_outline_safe_stop_release_report.json`。
- 线上截图：`output/playwright/online-87e115e-announcement-desktop.png`、`output/playwright/online-87e115e-announcement-mobile.png`。
- 已知无关资源：浏览器会请求根路径 `/favicon.ico` 并收到 404；当前页面使用已发布的 SVG favicon，本轮不改动无关 favicon 路由。
