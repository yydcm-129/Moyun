# 网页版中转多模型配置与切换生效执行日志

日期：2026-07-15
状态：已上线并完成线上核验

## 目标

- 同一 API 配置仅修改默认模型时，可以保存并在下一次模块请求中使用新模型。
- 对同一中转站 Key 可从现有配置派生多个“连接相同、模型独立”的 API 配置，供模块模型分配分别使用。
- 连接中心保持每个配置只配置一个模型，避免一个配置内部多选模型导致路由歧义。

## 已知根因

- 当前修改模型会重置测试状态；保存逻辑要求测试状态为 `ok`，因此未重测时保存失败、旧档案继续被请求层读取。
- OpenAI 兼容协议的“测试连接”只访问 `/models`，验证连接而非所选模型。
- 请求层不缓存模型：保存后的配置会被每次模块请求重新解析。

## 实施边界

- 只修改 Pages 仓的 `source/moyun.single.html` 与构建产物。
- 不修改冻结离线单文件、白鸟 JSON、历史交付与既有未跟踪测试文件。
- 模型切换不应自动更换地址、Key 或协议；用户需通过派生配置/模块分配显式切换连接线路。
- 单个模块的独立配置失败时仍直接显示上游错误，不使用其他配置兜底。

## 计划

1. 将连接验证状态拆分为“连接已验证”与“当前模型已验证”；模型单独修改允许保存，连接字段修改仍要求重测。
2. 为编辑中的模型添加最小真实模型验证；OpenAI 兼容协议保留模型发现，但模型验证走当前模型的 completion 请求。
3. 增加紧凑的“派生模型配置”入口，复制协议、地址、本机凭据与连接验证状态，仅要求填写/选择新模型。
4. 多样本抓包覆盖：未保存旧模型、同配置已保存新模型、同 Key 派生双配置、模块跨配置切换、不同协议错误直通；完成桌面/手机 UI 回归和 Pages 发布验证。

## 记录

- 已读取只读排查日志，确认复现结果与用户反馈一致。
- 已实现：修改默认模型只重置模型验证状态，不再清空地址、Key、协议对应的连接验证状态；保存后请求层实时解析档案的 `defaultModel`，不会继续使用旧模型。
- 已实现：默认模型右侧增加“验证模型”，使用当前模型发送最小真实请求；OpenAI 兼容协议命中 `/chat/completions`，Gemini 命中对应的 `models/<model>:generateContent`。
- 已实现：已验证配置可“派生模型”，复用协议、地址与本机 Key，新配置仍只保存一个模型，可独立被模块模型分配选择。
- 本地拦截回归 `output/connection_multimodel_final_regression.cjs`：通过。未发出真实供应商请求。
  - 同一配置 `glm-4 → gemini-3.5`：模型验证和模块请求均发送至 Alpha 中转，`model=gemini-3.5`、`Authorization=Bearer alpha-key`。
  - 派生配置：保存 `gemini-2.5-flash`，复用 Alpha Key 与已验证连接状态。
  - 跨配置：请求发送至 Beta 中转，`model=glm-4-plus`、`Authorization=Bearer beta-key`；故意返回 429 后无 Alpha 回退。
  - Gemini：模型验证与模块调用均命中 `.../models/gemini-3.5-pro:generateContent`。
- 桌面与 390×844 手机截图已保存；手机页面 `scrollWidth=390`，无横向溢出，默认模型输入宽 288px、验证按钮宽 62px。
- 发布公告：将公告 ID 更新为 `web-2026-07-15-connection-model-routing-fix`；标题“连接中心模型切换修复”，说明同配置模型切换实时生效及“派生模型”的多配置使用方式。用户刷新网页版即可看到。
- 构建：`npm run build`、`node --check assets/js/moyun.js`、`git diff --check` 均通过；修复构建 JS 指纹为 `0810debcf534`，公告更新后的指纹为 `63c404b3226b`。
- 发布：修复提交 `4852277` 与公告提交 `9ebde75` 均已推送至 `main`；GitHub Pages 工作流 `29355059355`、`29355280016` 均为 `success`。
- 线上回归：以 `https://not1a-del.github.io/Moyun/` 运行同一套拦截测试，所有样本通过、零页面错误；同配置新模型、派生配置、跨配置、429 不回退及 Gemini 端点均与本地结果一致。
- 线上公告：页面实际加载 `./assets/js/moyun.js?v=63c404b3226b`；桌面与 390×844 手机均显示“连接中心模型切换修复”，手机 `scrollWidth=390`，无横向溢出。
