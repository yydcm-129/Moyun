# Moyun 历史测试报告与当前交接检查

## 报告范围

本报告合并此前各轮交付中已执行的检查，并区分“已实际验证”“静态验证”和“尚未完成”。浏览器检查使用用户已打开的 Edge 页面；未修改示例文件 `F:\yydcm129\codex 自用小说软件\图片和文件\绘图风格2.0.txt`。

## 通用检查命令

在项目根目录 `F:\yydcm129\codex 墨韵接手\Moyun` 执行：

```powershell
npm run build
node --check assets/js/moyun.js
git diff --check
```

结果：v0.0.4、v0.0.5、v0.0.6、v0.0.7 均记录为通过；本次 v0.0.8 文档轮也已重新执行并通过。构建器从 `source/moyun.single.html` 生成 CSS、JS 和静态入口，不应直接编辑生成文件。

## 版本测试归档

### v0.0.4

- 静态合同断言：通过。覆盖卷纲滚动/收起、最新大纲读取、补充流式、字数不足不阻塞、无进度悬浮球、事件/角色 AI 填充和标准按钮。
- 构建与语法：通过。
- 限制：未完成真实模型 API 的全量端到端调用；部分自动填充逻辑是本地保守提取，不代表每个空字段都会自动发起 AI 请求。

### v0.0.5

- 构建、JavaScript 语法、差异空白：通过。
- 静态检查确认卷纲约束、正文优先细纲、全局超时/流式路径和工作台补充入口存在。
- 限制：生图和 AI 生成依赖用户自己的连接中心配置，未在离线检查中伪造 API 成功结果。

### v0.0.6

- 构建、语法和静态合同：通过。
- 全局搜索确认进度悬浮球相关代码已移除；细纲主要操作入口已改为标准 `<button>`。
- 限制：Impeccable 检测因环境缺少 `htmlparser2`、`css-select`、`css-tree`、`domutils` 进入正则降级模式，结果是欠报告，不能视为完整视觉审计。

### v0.0.7

#### 桌面端浏览器检查

- Edge 页面：`http://127.0.0.1:4173/index.html`。
- 视口：约 `1512×741`。
- Logo：4 个 SVG 图片 `complete=true`，`naturalWidth=150`，破损图片数为 0。
- 页面宽度：`document.documentElement.scrollWidth === innerWidth`，无水平溢出。
- 卷纲：6 个临时卷纲可在 `.moyun-volume-list` 内部滚动，`overflow-y:auto`，滚动高度大于可视高度。
- 收起/展开：点击“收起全部”后所有卷折叠；点击“展开第2卷”后第2卷恢复编辑区域，其余卷保持折叠。修复前该场景失败，修复后通过。
- 测试数据：临时新增卷纲已删除。

#### 移动端浏览器检查

- 视口：`390×844`。
- 首页、设定、角色、大纲、细纲：文档宽度均为 `390px`，无水平溢出。
- 触控：顶部主要按钮为约 `44×44px`；卷纲操作按钮满足移动端可点击尺寸，摘要编辑按钮高度约 `38.8px`。
- 卷纲：列表保持固定高度并支持内部滚动，移动端不会把大纲正文区域无限挤出视口。
- 细纲：`AI生成细纲`、`从正文反向生成`、`AI补充空白` 均为标准按钮，移动端未发现裁切或越界。
- 页面截图：已完成首页、设定、角色、大纲、细纲和卷纲收起状态截图检查；截图保留在本轮浏览器检查记录中，未写入项目目录。

#### 控制台与资源

- 未发现运行时 `error`。
- 仅出现项目原有警告：`cdn.tailwindcss.com should not be used in production`。本轮未添加依赖或改动 CDN，避免扩大范围。
- 临时 Node 静态预览服务已停止，端口 `4173` 已释放。

## 当前 v0.0.8 文档轮验证

- `package.json` 版本已由 `0.0.7` 递增至 `0.0.8`。
- 新增文档均位于 `docs/`，未改动示例文件。
- Markdown 标题、链接和代码围栏已人工检查；项目运行时代码未改动。
- 当前工作树在提交前应只包含本次文档和版本元数据变更。

## 当前 v0.0.9 验证（分支 `temp/v0.0.8-v009-work`）

### 构建与静态检查

- `npm run build`：通过，`assets/js/moyun.js` 版本指纹 `87808a515113`，`assets/css/moyun.css` 指纹 `e88d336e5bf1`。
- `node --check assets/js/moyun.js`：通过。
- `git diff --check`：通过，无空白错误。
- 提示词/标记字符串核对：构建产物（JS 逻辑层 + HTML 模板层）共 20 项关键字符串全部命中（覆盖 ①卷纲必选小节与行格式、③必填与推断标注、⑥摘要规则与禁尾评、⑦时间规则与格式约束、②整体收起函数）；正文内联“读者评论随写”指令残留 0 处。

### 单元级逻辑验证（Node 提取源内函数直接执行）

- 摘要多格式识别（⑥）：8/8 通过——`---剧情摘要---`、`## 剧情摘要`、`**本章摘要**`、`【剧情摘要】`、裸“摘要”、`Summary`、`--- 摘要 ---` 均被切分为摘要尾，无标记时返回空。
- 卷纲更新解析（①）：5/5 通过——标准格式逐卷回填（卷名+摘要）、章节范围修改生效、超出既有卷数的行被忽略、半角分隔变体兼容、无【卷纲更新】小节时不改动。
- 字数硬限制（③④⑤）：6/6 通过——`EVENT_FIELD_MAX` 表（title≤20/timeText≤24/summary≤60/cause·result·legacyImpact≤80）、事件回填使用该表、摘要 120 字硬截、态度 30 字截断、事件标题 120 字截断均存在。
- 剧情时间链路（⑦）：6/6 通过——`storyTimeOutputRule` 在 `buildChapterMessages` 内定义、以上一章 `getSnowwingChapterStoryTime` 为开关、注入位置在标题行之后、前文上下文注入剧情时间、新章保存与重掷路径写入 `storyTime`。
- 剧情时间格式归一（⑦）：7/7 通过——`【云历三年九月初七·黄昏】`、`【翌日清晨】`、`【当日午时三刻】` 被接受；`【月夜】`（无历法/锚点）、无括号、超 96 字被拒绝。
- 残留“读者评论”引用复核：23 处全部为合法用途（解析正则、独立评论生成提示词、禁止性表述、设置项说明）。

### 桌面端浏览器检查（Edge 无头 + CDP，视口约 `1482×805`）

- 启动：应用挂载成功，`开始使用/我知道了` 引导可关闭；控制台无 `error`，无未捕获异常。
- 导航：设定/角色/大纲/正文/预设各页切换正常，`scrollWidth === innerWidth`，无水平溢出。
- 卷纲板（②）：新建 2 卷后“整体收起”按钮可见；点击后卷纲列表消失、仅剩一条“卷纲已整体收起 · 2 卷”摘要行（含逐卷范围文本）+“展开卷纲”按钮；点击后完整卷纲板恢复且 2 卷仍在。“收起全部”+ 单卷“展开”的 v0.0.7 行为不受影响（单卷可见输入框恢复）。
- 事件时间线（④）：新建事件后“折叠列表”按钮可见；折叠后时间线仅一条摘要节点（显示项数与选中事件），事件节点隐藏；点击摘要节点恢复展开。事件标题 `maxLength=120`、占位“建议 20 字以内”，时间文本 `maxLength=40`。
- 关系（⑤）：角色工作台新建角色→切“常规”档案→添加关系后，“当前态”输入框 `maxlength=30`、占位“信任、疏离、试探…（30字内）”正常渲染。
- 截图：卷纲整体收起、事件折叠状态已留存（`.ui-check/results/`，不入库）。

### 移动端浏览器检查（CDP 设备仿真 `390×844`）

- 视口 `390×390`：无水平溢出；设定/角色/大纲/预设各页均无水平溢出。
- 移动端入口为“③ 编写大纲”向导按钮（侧栏页签在移动布局下不承载工作台开关）：点击后大纲工作台打开，卷纲板渲染，新增卷后“整体收起”可用；收起后摘要行显示“卷纲已整体收起 · 3 卷”，无水平溢出；“展开卷纲”恢复正常。
- 控制台无 `error`。

### 测试环境说明

- 本轮浏览器检查使用本机已有 Edge 以 `--headless=new --remote-debugging-port=9222` 启动（无下载、无新依赖），配合项目 `.ui-check/` 下的只读 CDP 客户端脚本执行普通用户级点击/输入；页面数据为临时空书与测试角色/事件/卷，未触碰用户真实作品。
- 临时静态预览服务（Node 内置 http，端口 `4173`）与调试用 Edge 将在交付后停止；`.ui-check/` 为未跟踪草稿目录，不提交。

## 已知风险与未完成验证

1. 真实 AI API、生图 API、NAI 5 返回图片和超长正文评论生成需要用户配置的在线服务，不能仅凭静态构建判定成功。
2. 本轮 ①③④⑤⑥⑦ 的核心是提示词与格式约束，离线环境只能核验“指令已注入、解析与硬限制可执行”，无法在未发送真实 API 请求（被禁止）的前提下验证模型的实际服从度；建议用户接入真实模型后观察前两章的卷纲回填、补全空白、摘要长度与剧情时间输出。
3. ⑦ 剧情时间链路仅在上一个上下文章节带有合法【...】时间时才要求模型续写本章时间；新书或时间线缺失时不会凭空生成时间，这是有意设计而非遗漏。
4. 摘要 120 字硬截是兜底：若模型持续无视 60-100 字提示词，长摘要会被截断而非报错，用户应关注生成质量而非依赖截断。
5. Tailwind CDN 生产警告仍存在；改为本地构建需要引入依赖，必须单独获得授权。

## 下一轮测试门禁

修改运行时代码后至少执行构建、语法和差异检查；涉及 UI 时还要在 Edge 真实页面检查桌面和 `390×844` 移动端，确认无水平溢出、资源破损、控制台错误和按钮裁切。涉及 AI/API 时必须记录请求模式、超时、流式状态和错误处理结果，不要把“按钮存在”当成接口成功。
