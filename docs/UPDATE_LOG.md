# Moyun 更新日志

> 本文件记录当前接手工作区可从 Git 历史和既有交付记录核实的版本变化。版本号遵循“只提升最后一位”的规则。

## 版本总览

| 版本 | 日期 | 提交 | 主要内容 | 回滚参考 |
| --- | --- | --- | --- | --- |
| v0.0.0 | 基线 | `995fe90` | 当前仓库可追溯的初始稳定基线 | `v0.0.0` 分支 |
| v0.0.1 | 基线别名 | `995fe90` | 当前历史没有独立提交，沿用基线 | `v0.0.1` 分支 |
| v0.0.2 | 基线别名 | `995fe90` | 当前历史没有独立提交，沿用基线 | `v0.0.2` 分支 |
| v0.0.3 | 基线别名 | `995fe90` | 当前历史没有独立提交，沿用基线 | `v0.0.3` 分支 |
| v0.0.4 | 2026-09-01 | `5b4ff0d` | 写作流程、上下文、补写、工作台与 UI 改进 | `v0.0.4` 分支 |
| v0.0.5 | 2026-09-02 | `967e9bf` | 卷纲/细纲框架、正文优先上下文与相关交互增强 | `v0.0.5` 分支 |
| v0.0.6 | 2026-09-02 | `e120598` | 卷纲、工作台 AI 补充、事件/状态同步、移除悬浮球 | `v0.0.6` 分支 |
| v0.0.7 | 2026-09-02 | `0ae8d44` | 修复“收起全部”后无法单独展开卷纲 | `temp/v0.0.6-v007-work` |
| v0.0.8 | 2026-09-02 | `87a1bad` | 本次新增交接文档与历史测试归档，仅更新版本元数据 | `temp/v0.0.7-v008-docs-work` |
| v0.0.9 | 2026-09-02 | `1caab89` | 卷纲AI识别、卷纲整体收起、AI补全防空白、事件折叠与字数约束、关系态硬限制、摘要分类与剧情时间链路 | `temp/v0.0.8-v009-work` |
| v0.0.10 | 2026-09-03 | `ecb4fc5` | 热更新：事件时间线UI修复、工作台逐字段流式回填、事件AI逐章阅读全文、卷纲逐章大纲、细纲按正文比例、回归脚本；同日补充轮（见下）不改版本号 | `temp/v0.0.9-v010-work` |
| v0.0.11 | 2026-09-08 | 待提交 | 角色工作台双端渲染核验、台词整句识别、细纲流式落卡、改写要求保留、生图重试按钮、事件时间线自动补录开关、百万字序列化瘦身 | `temp/v0.0.11-work`（基线 `backup/v0.0.11-base-60f5921`） |

## v0.0.10 补充轮上线公告（同日第二次追加，仅公告内容，版本号不变）

分支：`temp/v0.0.10-announce-publish`（基于补充轮 `038b967`）。按用户要求仿照既有弹窗更新公告格式，只写本轮补充的四项更新内容，随后推送 GitHub Pages 上线。本轮只改 `WEB_UPDATE_ANNOUNCEMENT` 常量（id `web-2026-09-03-2230-0810-supplement-update`，标题「0810更新·补充」，发布时间 2026-09-03 22:30）：四条条目分别对应补充①「全文时间线补充」按钮改名+范围弹窗、补充②时间线填对再排序、补充③「已补充到第几段」进度小字、补充④细纲比例 1:4~1:5，替换旧 0815 公告内容（公告常量是单份覆盖式，不追加历史）。用户已读旧公告 id 的会重新弹新公告；全新浏览器仍先看新手说明再看本公告。

## v0.0.0～v0.0.3：基线阶段

当前仓库的 Git 历史中，这四个版本分支均指向 `995fe90`，没有可单独归属的版本提交。因此不能从仓库证据中声称这四个版本存在不同功能；后续接手者应将 `995fe90` 视为共同基线。

## v0.0.4：写作工作流改进

提交：`5b4ff0d feat: release v0.0.4 writing workflow improvements`

- 修复影响文件上传和静态网页的历史致命问题，并重新构建静态入口。
- 完善字数检测：AI 内容不足时支持二轮补写，补写期间继续累计计时、总字数和每秒字数；流式请求优先。
- 强化角色设定、资料条目、大纲等上下文的识别和注入；反向生成细纲时读取大纲、角色与相关设定。
- 创作设定工作台增加 AI 补充能力；资料条目支持自定义类型和别名/类似词调用。
- 生图设置增加图片数量、检测逻辑及画师串更新能力；未修改用户提供的示例文件。
- 增加并完善卷纲硬性边界、细纲分层、超时保护和全局流式策略。
- 增加角色当前状态与事件时间线的自动记录/辅助记忆逻辑。
- 移动端续写框全选删除后恢复焦点，操作入口逐步统一为标准按钮。

## v0.0.5：卷纲与上下文工作流增强

提交：`967e9bf feat: improve volume outline and context workflow`

- 卷纲成为大纲的默认/必选层级，支持卷标题、章节范围、摘要编辑及放大编辑。
- 卷纲列表设置内部滚动和“收起全部/展开全部”，并把卷纲约束注入正文与细纲提示词。
- 细纲生成强制读取最新大纲；正文存在时按正文与事件时间线优先生成后续细纲。
- 二轮补写结束后直接输出，不再因仍未达标而进入警告死循环，也不再追加“（未完）”。
- 二轮补写继续累计计时、正文总字数和每秒字数。
- 角色、事件时间线、大纲、资料条目增加 AI 补充入口，并支持流式预览。
- 完成若干静态合同检查、JavaScript 语法检查和构建检查。

## v0.0.6：工作台补充与悬浮球移除

提交：`e120598 feat: 完善卷纲与工作台补充并移除进度悬浮球`

- 完善卷纲默认建立、卷纲更新解析、跨卷硬性边界提示和卷纲编辑器。
- 细纲增加“AI生成细纲”“从正文反向生成”“AI补充空白”等标准按钮入口。
- 正文结束后进行角色状态、资料摘要和事件时间线的保守提取/同步。
- 资料条目、事件时间线、角色工作台和大纲工作台增加流式 AI 补充入口。
- 删除进度悬浮球的 HTML、CSS、JavaScript、事件监听和相关引用。
- 评论/回复点赞等可点击入口改为真实 `<button>`，降低交互和无障碍风险。

## v0.0.7：卷纲单项展开修复

提交：`0ae8d44 fix: allow individual volume expansion after collapse all`

### 问题

点击“收起全部”后，单独点击某一卷的“展开”按钮没有效果，用户无法查看或编辑该卷。

### 修复

`toggleNovelVolumeCollapsed` 在全局折叠状态下现在会退出全局折叠，并只保留当前卷为展开状态，其余卷继续折叠。卷纲内部滚动逻辑保持不变。

## v0.0.8：交接文档归档

本版本仅补充项目交接材料并递增版本元数据，不改变运行时代码：

- 新增本文件，归档每个可追溯版本的更新内容、提交和回滚位置。
- 新增 [TEST_REPORT.md](./TEST_REPORT.md)，归档静态检查、浏览器检查、桌面端/移动端检查及已知限制。
- 新增 [HANDOFF.md](./HANDOFF.md)，说明源码唯一来源、构建流程、版本分支和下一轮接手步骤。

## v0.0.9：卷纲识别、防空白补全与摘要/时间链路修复

分支：`temp/v0.0.8-v009-work`（基于 v0.0.8 稳定副本 `c6f69f4`）。本轮按用户 7 项需求逐项修改，全部改动集中在 `source/moyun.single.html`，构建后同步生成产物。

### ① 大纲工作台的卷纲AI识别

- `buildStructuredOutlineInstruction` 把【卷纲更新】改为必选小节，固定逐卷一行格式 `第N卷｜卷名：XXX｜章节 X-Y｜框架摘要：XXX`，无改动也照抄当前值；卷数与章节边界必须与【现有卷纲】一致。
- `generateOutline` 在【现有卷纲】后注入【卷纲输出要求】，要求大纲文本最前面输出该小节；`aiSupplementOutline` 同样约束补齐空白结构。
- `applyAiVolumeUpdates` 重构为先收集再回填：按行解析卷号/卷名/章节范围/框架摘要，超出既有卷数的行被忽略（卷数量不交给模型扩容）；大纲请求统一 `preferStream: true`。
- “AI修改大纲”复用同一提示词链路，修改意见明确涉及卷纲时可更新对应卷。

### ② 卷纲UI：整体收起

- 新增 `novelVolumesBoardMinimized` 状态与 `toggleNovelVolumesBoardMinimized`，卷纲板头部新增“整体收起”按钮。
- 收起后整块卷纲板替换为一条摘要行（“卷纲已整体收起 · N 卷”+逐卷范围），点击“展开卷纲”或摘要行即可恢复；放大编辑快捷行在收起时隐藏。
- 与既有“收起全部/展开全部”互不干扰：整体收起更深一层，单项展开逻辑保持 v0.0.7 行为。

### ③ AI补全不留空白

- `aiSupplementStoryBible`：project 五字段和 worldView 必填，禁留空/禁写“无/暂无/待定”，素材不足须合理推断并标注“（据现有信息推断）”；entries 至少 3 条。保存后仍有空字段时宿主兜底写入占位说明，工作台不再出现“AI已补充却一片空白”。
- `aiSupplementSelectedCharacter`：8 个档案字段全部必填，currentState ≤60字、其余 30-120 字。
- `aiSupplementStoryBibleEvent`：6 字段全部必填（标题≤20/时间≤24/摘要≤60/因果≤80 字），新增 `EVENT_FIELD_MAX` 常量表并在回填处硬截断。
- `aiSupplementStoryBibleEntry`：name/summary/details 必填并有字数上限。

### ④ 事件时间线自动填充约束

- 时间线头部新增“折叠列表/展开列表”按钮；折叠后仅一条摘要节点（项数+当前选中事件），点击展开。
- 事件标题输入 `maxlength=120`、占位提示“建议 20 字以内”，时间文本 `maxlength=40`；`normalizeStoryBibleEvent` 保存层再截标题 120 字。
- AI 事件补充提示词按 ③ 的字段上限约束输出，避免超长标题挤爆阅读区。

### ⑤ 关系图“当前态”字数硬限制

- `normalizeCharacterRelationships` 保存层把 attitude 统一截到 30 字、changeReason 截到 120 字，任何来源（AI、导入、草稿回填）都不会超限。
- 三处输入框（原卡编辑器、角色工作台当前态、草稿审阅）加 `maxlength=30` 与“（30字内）”提示；AI 提示词改为“态度描述(20字以内)”。
- `.moyun-rel-fact-state` 由 `nowrap` 改为自动换行 + `max-width:52%`，弹窗内不再溢出。

### ⑥ 正文摘要分类修复

- 正文生成不再要求模型连带输出读者评论：7 处提示词（主 sysPrompt、续写、修复、白鸟回读、预检、finalCheck、白鸟承载）全部改为“摘要结束后立即停止”，评论仅保留独立的 `generateChapterComments` 调用。
- 摘要提示词统一为 60-100 字；`parseChapter` 保存层新增 120 字硬截断，防模型把书评误灌进摘要。
- `findLastSummaryMarker` 识别范围扩展：`---剧情摘要---` 分隔线、`## 剧情摘要` 标题、`**本章摘要**` 加粗、`【摘要】`、裸“摘要”行和 `Summary` 均可正确切分。

### ⑦ 剧情时间进入AI链路

- `buildChapterMessages` 新增 `storyTimeOutputRule`：前文上下文（已有 `formatSnowwingChapterStoryTimeContext` 注入“剧情时间（独立元数据）”行）中存在上一章剧情时间时，要求模型先读取上一章时间、结合本章正文推进时间，并在标题行前单独一行输出本章时间，格式固定 `【历法或相对时间·时段】` 且须含日期数字或“当日/翌日”锚点，整行 ≤40 字。
- 新书/无时间线时不注入该规则，避免凭空造历法。
- `parseChapter` 既有时间行剥离逻辑负责识别该行，章节保存与重掷路径写入 `storyTime`，编辑模式“剧情时间”字段继续有效。

## v0.0.11：七项修复与新增——工作台核验、台词整句、流式落卡、输入保留、生图重试、时间线自动补录、大书瘦身

分支：`temp/v0.0.11-work`（基于 v0.0.10 上线公告轮 `60f5921` 与 AGENTS 修订 `20c8312`；回滚基线分支 `backup/v0.0.11-base-60f5921`）。错误来源为用户提供的截图目录 `F:\yydcm129\codex 墨韵接手\错误报告和截图`（角色工作台错位、台词识别错误两组截图）。全部业务改动集中在 `source/moyun.single.html`，`scripts/regression-check.cjs` 仅随修复③更新锚点，构建后同步生成产物。

### ① 角色工作台双端渲染核验（紧急）

- 用户截图报告“角色工作台严重错位、完全无法阅读”。对当前构建产物做了真实浏览器渲染验收（headless Edge + CDP，桌面 1280×800 与移动 390×844）：注入测试角色 → 打开角色分类 → 点击可见角色行进入工作台，断言编辑器头部为 `moyun-character-editor-head` 的直接子级、档位选择器不在头部子树内、整页无横向溢出、无 fixed 越界、无控制台错误。桌面 8/8、移动 8/8 全部通过——当前 v0.0.10 构建产物的工作台在两端均渲染正常，截图中的错位在本轮环境未能复现。
- 同时按用户要求扩展审计了全部二级三级界面：设置弹窗 6 个标签页（连接中心/上下文/书籍与提示词/插件/生图/关于）与设定工作台 5 个分区（项目总览/世界总览/事件时间线/资料条目/上下文包），PC 与手机两种视口共 22 项检查全部通过（只把横向溢出、fixed 越界算作错位；可滚动容器的纵向滚动属正常行为）。
- 已知边界：截图中的错位可能依赖特定数据（超长角色名/状态/关系）、特定缩放或特定浏览器版本触发，本轮环境未能复现。若用户在真实环境仍能看到错位，请提供复现步骤与角色数据规模，下一轮针对性修复。

### ② 台词示例整句识别

- 根因：`normalizeStringList` 原来对任何列表字段都用同一套 `/[、,，\n]+/` 切分，台词里的顿号、逗号被当成标签分隔符——“你以为，你真的逃得掉吗？”被拆成“你以为”“你真的逃得掉吗”等碎行填入台词示例。
- 修复：函数增加第三参数 `mode`。`tags`（默认）保持原顿号/逗号拆分，供性格标签等短词字段使用；`dialogues` 模式只按换行分条，条内多句再按句末标点（。！？!?…）拆成完整句子，逗号/顿号永不拆句。四处台词相关调用（AI 生成载荷归一化 ×2、工作台草稿构建 ×1、草稿应用 ×1）均改为 `dialogues` 模式，其余列表字段不受影响。
- 这是一种长久稳定的格式约定：宿主端解析规则固定且只认“换行=新台词、句末标点=长台词断句”，不依赖模型记住“不要用逗号”之类的提示词。

### ③ 细纲流式落卡

- 根因：批量细纲虽然请求走流式，但 `onTextDelta` 只把全文存进 `outlineStreamPreview` 供预览区滚动显示，章节卡片要等整批完成、最终解析后才一次性出现。
- 修复：新增 `ensureStreamedChapterCard`（按章号补齐/建卡并展开）与 `applyDetailedOutlineStreamDelta`（增量解析：扫全文里所有细纲标题行，按批内章号落进对应卡片正文；`onlyEmpty` 场景用临时标记 `_streamed` 保护已有内容不被半截流覆盖）。`onTextDelta` 每次回调即落卡，AI 每输出一章标题、对应卡片立即出现并持续填入。预览区改为只显示批进度与落卡说明，不再整段粘贴原文。
- 临时标记在批次合并后与任务结束（含失败/停止）时清理，不会存进存档。最终整段解析校验逻辑不变，流式只是显示层。
- 其他可流式的区块（大纲、四类工作台补充、事件批量）在 v0.0.10 已逐字段流式回填，本轮核查确认无遗漏。

### ④ 改写要求输入保留

- 大纲生成、单章细纲、批量细纲三处成功后原来直接清空输入框（`outlineInput.value=''`、`target.aiInput=''`、`doInput.value=''`），用户无法核对上一条要求、也无法原样再发。
- 修复：三处清空语句移除，发送后输入保留，用户可手动删除或修改后再发。明确放弃草稿的唯一入口仍是对话框里的“放弃修改”（`discardDetailedOutlineNavigationDraft`，用户显式点击才触发），该语义保留不动。

### ⑤ 生图区块重试按钮（新增）

- 正文内已生成图片右下角新增小重试按钮（30px 圆形、半透明深底白字 ↻，锚点 `data-img-action="retry-regen"`）。PC 端悬停图片才出现（`opacity:0 → 1`），触屏设备（`@media (hover:none)`）常驻但保持 0.78 半透明，不遮挡画面主体。
- 点击走既有 `data-img-action` 全局委托，先弹二次确认（取消/重新生成），确认后才携带原 cacheKey 与 tag 调用 `regenerateInlineImage` 重新生成；取消不产生任何请求。

### ⑥ 事件时间线自动补录开关（新增，默认开）

- `settings.autoTimelineSupplement` 默认 `true`：每章正文结束后自动提取事件写入时间线的行为照旧；旧存档加载时补上默认值，升级后行为与全新安装一致。
- 开关为运行时即时读取：正文生成路径 `updateStoryMemoryFromChapter` 在写入事件前检查开关，上一章开、本章关这类交错操作各自按当次状态生效，不会因缓存旧值误写或漏写。关闭时只跳过事件补录，角色状态同步、资料摘要填充等其余记忆逻辑不受影响。
- 两处 UI 共用同一状态：事件时间线工作台顶栏（`data-story-event-auto-toggle`）与设置→上下文页（`data-settings-auto-timeline`），均复用既有 `toggle-track` 开关样式，PC/移动端通排。

### ⑦ 百万字级大书序列化瘦身

- 根因：每次自动保存对全书做两轮完整深拷贝（`syncBookData` 一份进 books、`buildLibrarySnapshot` 再一份进快照），随后 `JSON.stringify` 又序列化一次——百万字书每次防抖保存瞬时多占约 3 倍内存，Chrome 反复保存后内存峰值飙升可致崩溃。
- 修复：新增 `snapshotForSerialize`（浅结构展开 + 叶子值引用共享，层级超过 12 的极端结构回退 JSON 深拷贝）替代快照构建里的 `deepClone`。快照的三个消费方都只做 `JSON.stringify` 或只读（IndexedDB 写入、beforeunload 兜底、应急备份），序列化结果与深拷贝值层面完全一致，故引用共享安全；`syncBookData` 仍走 `deepClone`，跨书隔离不变。
- 运行时数据只存在一份，保存路径不再产生双倍大对象拷贝。

## v0.0.10：热更新——事件时间线、流式补全、逐章卷纲与比例细纲

分支：`temp/v0.0.9-v010-work`（基于 v0.0.9 稳定副本 `225edf5`）。本轮按用户 6 项需求逐项修改，全部改动集中在 `source/moyun.single.html` 与新增 `scripts/regression-check.cjs`，构建后同步生成产物。

### v0.0.10 补充轮（同日追加，版本号不变，基线 `4fd39a4` / 备份分支 `backup/v0.0.10-supplement-base`）

按用户 4 项补充需求修改，只改 `source/moyun.single.html`、`scripts/regression-check.cjs` 与本目录三份文档，构建后同步生成产物。

- **补充① 全文时间线补充**：事件时间线顶栏“AI补充”改名“全文时间线补充”（锚点 `data-story-event-ai-full`）。点击不再立即补充，而是弹出页面内弹窗（`data-moyun-modal="story-event-range"`，与既有 `showInputPrompt` 同一套 `pop` 过渡/z 层级规范），让用户选“起始章~结束章”（默认 1 到最后一章，超出范围提示“章节范围超出已有正文（1-N）”）；提交后只阅读所选范围的章节全文，按 3 章一批循环生成。取消按钮可关闭弹窗。
- **补充② 时间线填对再排序**：事件补充提示词新增硬规则——`timeText` 必须准确反映该事件发生在本章正文中的时间点（开头/中段/结尾/当日/翌日/天数推移），禁止照抄章节号；补充完成后按 `sortOrder`（=章号）稳定重排整条时间线，无章号事件排在末尾且相对顺序不变。
- **补充③ 段进度小字提示**：新增全局 `aiSupplementSegmentProgress` ref（同一时刻只有一个补充任务在跑）。AI 连接确认并开始输出后，按钮下方出现小字“已补充到第 X/Y 段”：工作台四类补充按已填字段计数（设定 6 段/条目 2 段/事件字段 5 段/角色档案 8 段），事件批量按批次计数（“第 X/Y 批（第 A-B 章）”），细纲按批计数，提示词流水线按层计数，大纲/逆向大纲按已输出段落数计数；首次收到文本先显示“已连接，正在输出…”。全部 8 处 AI 补充按钮（流水线、设定总补充、事件编辑、条目编辑、角色编辑、大纲行、细纲页脚、事件顶栏）均挂载提示，任务开始清空、结束（含失败）清空。
- **补充④ 细纲比例 1:4~1:5**：删除原 6% 启发式，改用用户实测比例的中值 1:4.5——每章细纲预算 = 所选正文字数 ÷ 4.5（每 1000 字细纲负责 4000-5000 字正文），批量下限 200 字/章、单章下限 500 字/章；手动改过“细纲字数”设置的用户仍固定在设置值（改回 3000 恢复自动比例）。设置面板（全局设置→上下文→细纲字数）说明文字同步更新为 1:4~1:5 口径。
- 回归脚本同步扩充：头部说明加 6)、7) 两项，新增 S1-S4 共 13 处锚点断言与 4 组比例抽样（600→200、2500→556、10000→2222、15000→3333），并断言旧 6% 比例已移除；回归 51 项全部通过。

### ① 上一轮已知风险处理

- 事件字段超限不再静默截断：硬截断发生时计数并弹 warning toast（“已按章插入 N 条事件…有 M 处字段超过建议字数，已自动截断到标题20/时间24/摘要60/其他80字”），与既有错误弹窗同级展示。
- “模型较老较小”风险按用户要求暂不处理。
- 新增可重复执行的全局回归脚本 `scripts/regression-check.cjs`（`npm run regress`）：构建 + `node --check` + 关键锚点存在性 + `applyAiVolumeUpdates`/`extractStreamFieldValue` 纯函数单元断言 + 字数硬限与比例抽样，替代一次性临时浏览器脚本。

### ② 工作台逐字段流式回填

- 新增通用助手 `extractStreamFieldValue`（JSON 键值与“字段名：值”行式两种抽取，支持未闭合半截流）与 `createWorkbenchStreamFiller`（按字段增长量逐个回调）。
- 四处工作台 AI 补充全部接入流式回填，模型写到哪个字段、输入框立即出字：设定总补充（premise/coreConflict/themeQuestion/toneNotes/narrativeRules/worldView）、资料条目（summary/details）、角色档案（desc/8 个档案字段）、事件时间线（title/timeText/cause/result/legacyImpact）。
- 细纲面板补上流式预览显示位（此前 `outlineStreamPreview` 在细纲任务中只写不显）：新增 `isGeneratingDO && outlineStreamPreview` 预览框，含批次号与范围。
- 最终解析写入逻辑不变：流式阶段只填空白字段，完成后仍走原 JSON 整段校验回填，避免半截内容固化。

### ③ 事件时间线 UI 错误修复

- 根因：`.moyun-event-fields.three` 用固定列宽 `minmax(0,1fr) 150px 170px`，长标题+长标签场景下标题列被压到 38-76px。
- 修复：改为比例列 `minmax(0,1.6fr) minmax(0,.6fr) minmax(0,.7fr)`，并给网格内 label/input/textarea/select 加 `min-width:0`、标签自动换行、编辑器容器 `min-width:0`。
- 验证：桌面 1482px 标题输入框 235px、移动 390px 单列 318px，均无横向溢出。

### ④ 事件时间线 AI 补充改为逐章阅读全文

- `aiSupplementStoryBibleEvent` 重写：按 3 章一批循环请求，每批携带该批章节完整正文（不再裁剪 16000 字），要求按章输出 JSON 数组事件。
- 插入顺序：`sortOrder = 章号`，事件自动关联对应章节（chapterIds），完成后按章号重排整条时间线。
- 守卫前移：无正文时先提示“暂无正文可阅读；请先写作或导入章节”，再检查 API 配置；空书/未配置都能得到正确的下一步指引。
- 超限字段截断计数并触发 ① 的 warning toast；中止时已插入事件保留。

### ⑤ 卷纲逐章大纲拼卷

- 【卷纲更新】格式升级：每卷先一行卷头（`第N卷｜卷名：XXX｜章节 X-Y｜框架摘要：XXX`），紧随其后按顺序每章一行“`第M章：一句话大纲（不超过40字）`”，章行必须覆盖该卷章节范围内每一章。
- `applyAiVolumeUpdates` 两遍扫描：先识别卷头行，再把章行归属到最近的既有卷头；卷摘要 = 框架摘要 + 逐章章行（换行拼接，上限 2000 字，原 120 字上限移除）。
- 单章大纲合在一起构成本卷卷纲、多卷卷纲合在一起构成完整总纲；旧格式（无章行）完全兼容，臆造卷号照旧忽略。
- 【现有卷纲】注入时摘要多行折叠为“；”连接，保持每卷一行输入格式。

### ⑥ 细纲按所选正文字数比例生成

- 批量细纲：总预算默认 = 所选正文字数 × 目标章数 × 6%（每章细纲≈正文的 6%，下限 600 字）；一键成书等显式传入 wordCount 时优先；用户手动改过“细纲字数”设置（≠3000）时按设置值。（本项 6% 比例已于同日补充轮替换为 1:4~1:5，见上方补充轮记录。）
- 单章补写/改写：每章细纲字数同样按 6% 比例（未动过设置时），替代原“设置值÷总章数”的平摊。（同上，已改为 ÷4.5。）
- 设置面板说明更新，注明默认比例与手动固定值的关系；比例数值抽样断言写入回归脚本。

## 版本与回滚规则

1. 每轮修改前从上一稳定版本创建临时分支。
2. 版本号只提升最后一位，例如 `v0.0.7 → v0.0.8`。
3. 修改源码优先于生成产物；运行 `npm run build` 同步 `index.html`、`assets/css/moyun.css` 和 `assets/js/moyun.js`。
4. 完成构建、语法、差异和必要的浏览器/响应式测试后，才可将临时分支作为新稳定版本。
5. 回滚时优先切换到对应版本分支或提交，不使用破坏性重置覆盖用户数据。
