# Moyun 项目协作规则

## 版本与发布

- 每一轮更新都必须递增版本号，并附带检测报告和更新说明。
- 版本号只提升最后一位：当前基线为 `v0.0.0`，下一轮为 `v0.0.1`，后续依次递增。
- 每一轮都必须基于前一轮的稳定副本进行更新；完成检测并确认无误后，才能作为新版本交付。
- 必须随时做好代码回滚；每次修改前必须先备份或创建并切换到新的版本分支。

## 操作安全

- 严禁未经用户允许启动、下载或运行任何不必要的软件。
- 不得修改与本轮需求无关的文件。
- 所有更新必须同时兼顾 PC 端和移动端，测试方法包括使用当前设备内已有的浏览器和将窗口调整到不同尺寸进行测试。

## 检测与交付

- 输出最终代码前，必须先完成review并确认无误后才能交付。
- 每轮交付必须同时提供检测报告和更新说明，注明测试范围、结果、已知问题及回滚位置。
- 应优先使用项目既有的构建、语法检查和回归检查流程；不得以未执行测试代替“确认无误”。
- 本项目是单文件 Vue 模板（`source/moyun.single.html`），改完模板必须跑 `npm run audit`（`scripts/structure-audit.cjs`），它专查肉眼和浏览器溢出检测都抓不到的结构性破坏。
- 检测报告必须注明结构审计（`npm run audit`）的结果；审计不通过禁止交付，即使功能测试全绿。

## 结构级坑与精准排障手册（v0.0.12 新增）

以下规则来自 v0.0.10 角色工作台错位的真实教训：模板少写 1 个 `</div>`，浏览器把后续兄弟节点全部吞进 flex 行，卡片被挤成 26~30px 竖条，而文档总宽不变——溢出检测恒为绿，功能测试恒为绿，bug 照常上线。

### 为什么常规检测抓不住这类问题

- “横向溢出”检测量的是 `document.scrollWidth`，容器内部挤压不改变文档总宽，恒为绿。
- 逐个功能点击测试只验证“元素存在、可交互”，不验证“元素在正确的父级里”。
- 长文本撑宽、min-width 缺失、MOD 主题污染都是**表象相似的错误方向**：先证明结构平衡，再查内容/样式。

### 遇到“卡片挤成细条 / 面板莫名变窄 / 空态和内容同屏”时的排查顺序

1. 先跑 `npm run audit`：标签平衡 + v-else 配对 + editor-head 锚点，一次定位缺失的闭合标签。
2. 用 CDP 抓运行时 DOM 验证：v-else 空态（`max-w-md mx-auto`）出现在编辑器内部 = 模板被浏览器改了层级。
3. 都干净才去查 CSS（flex/grid、min-width、MOD 注入）——顺序不能反，否则会把结构病当样式病修。

### 写模板时的硬性自查（每次改动模板后）

- 改动处 `<div>` 与 `</div>` 数量必须相等；嵌套拆行（如把单行按钮区拆成两层 flex）是高危动作，拆完必须数闭合。
- `v-else`/`v-else-if` 的前一个兄弟元素必须带 `v-if`/`v-else-if`；img 等 void 标签也算兄弟。
- 关键面板的头部行与其后续内容卡片（如 editor-head 与 moyun-character-levels）必须是同一父级的相邻子元素。
- 交付前在无 MOD 的干净构建上做一次“空白角色 + 超长无空格文本”双用例几何实测：卡片宽度不得小于容器宽度的 80%。

## 需求澄清

- 如果用户需求存在描述模糊、逻辑冲突或信息遗漏，必须立即停止写代码。
- 停止后列出 2 到 3 个具体可选方案，等待用户选择；严禁擅自猜测用户意图并直接动手。

## 结束后复盘和提问

- 详细告诉用户当前有什么最没有把握的事情
- 基于当前状况，用中肯的语气告诉用户当前最大的遗漏是什么？用户有什么问题没有发现和意识到