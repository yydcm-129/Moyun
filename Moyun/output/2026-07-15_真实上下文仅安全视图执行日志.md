# 真实上下文仅安全视图执行日志

日期：2026-07-15
状态：已发布并完成线上核验

## 用户要求

- 隐藏“真实上下文”窗口里的“原始排障”按钮。
- 用户只可见“安全视图”一个视图按钮，复制内容也只能是安全脱敏版本。
- 在干净 JSON 导出上线后，单独完成本项并上线。

## 变更边界

- 仅修改网页版真实上下文窗口的可见 UI、可见消息与复制路径。
- 不删除本机已经记录的上下文，不改生成请求、连接中心、白鸟规则、MOD 或其他界面。

## 实施计划

1. 打开真实上下文时强制回到安全模式，UI 只保留安全视图按钮。
2. 移除原始排障说明和 raw 分支；展示与复制函数始终调用脱敏映射。
3. 构建、检查桌面/手机弹窗与静态安全断言，推送并确认 Pages 更新。

## 本地验收结果

- 已重新构建网页静态产物，产物 JS 指纹为 `284ec612e45f`。
- 静态断言通过：页面不存在“原始排障”按钮和 raw 展示分支；只保留一个“安全视图”按钮；从工具栏打开窗口会强制设为安全模式；消息展示函数固定经过脱敏映射。
- Edge 实测在运行时刻意将旧状态设为 `raw` 后再打开窗口：页面仍不存在“原始排障”按钮，测试用白鸟规则/密文提示词不出现在 DOM，且只出现一个“安全视图”。
- 已在桌面与 `390×844` 手机视口检查。手机端标题信息与控制区上下排列，未再相互挤压；控制台无错误（仅既有 Tailwind CDN 开发提示）。
- 复制文本与复制 JSON 均固定读取安全脱敏消息，JSON 中 `viewMode` 固定为 `safe`。

## 发布记录

- 已提交并推送：`07a172a fix: restrict real context to safe view`。
- Pages 在线页面已引用 JS 指纹 `284ec612e45f`；线上 JS 核验确认：不含“原始排障”、不含真实上下文的 raw 展示分支，且固定包含安全脱敏映射与 `viewMode: 'safe'` 复制标记。
