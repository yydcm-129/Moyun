# 网页版完整开发文档编写执行日志

日期：2026-07-15
状态：已完成并发布

## 目标

- 为 `Not1a-del/Moyun` Pages 仓编写一份以当前在线源码为准的完整开发文档。
- 文档覆盖项目边界、目录、构建发布、页面模块、数据存储、连接中心、模型适配、公告、测试、安全边界，以及面向 MOD 开发者的包格式、生命周期、宿主 UI、事件、AI 工具、数据表与兼容要求。
- 文档将作为网页版本后续维护和 MOD 共创的交接入口，不以冻结离线单文件作为发布源。

## 边界

- 只读取冻结离线区中的历史接口资料，所有结论必须回到 Pages 仓 `source/moyun.single.html` 当前实现核对。
- 仅新增/更新在线仓文档与 README 文档入口；不改页面运行逻辑、离线单文件、白鸟包或既有未跟踪测试产物。
- 发布前执行 Markdown 链接/代码锚点检查、Git 差异检查，并推送 `main` 后确认 Pages 构建成功。

## 计划

1. 梳理在线仓目录、构建脚本、部署配置和现有维护记录。
2. 核对连接中心、持久化、公告和模块请求链路。
3. 核对 MOD 安装、规范化、事件、UI、AI 工具与桥接 API。
4. 编写完整文档、README 入口和本日志。
5. 校验、提交、推送并记录线上构建结果。

## 已完成内容

- 新增 `docs/ONLINE_VERSION_COMPLETE_DEVELOPER_GUIDE.md`，以当前 Pages 源码为准整理了唯一写入源、仓库结构、构建缓存、GitHub Pages 发布、运行架构、浏览器本地存储、导入导出、隐私、连接中心、四协议适配、严格模块路由、生成/公告/弹窗、MOD API 1.1、主题与白鸟 Skill 信任模型、测试矩阵、排错与最小示例。
- 在 `README.md` 增加完整文档入口，并提醒网页运行时只认连接中心、生成物不可手改、冻结离线文件不可作为发布源。
- 特别记录当前接口的真实限制：普通 MOD 走声明式安全层；Full Access 主题和白鸟 Skill 运行时属于高风险例外；`sidebarTool` 接口当前不直接渲染；普通 MOD 导出暂不保留 `activeContextTools`；遗留 `settings:moduleModels` 不能作为网页连接中心路由配置。

## 校验记录

- `npm run build`：通过；构建器输出 CSS 指纹 `d564d41b9f2b`、JS 指纹 `3a2aa34b6654`。
- `node --check assets/js/moyun.js`：通过。
- Markdown 结构检查：通过；完整手册 732 行、45,345 字节、38 个围栏（成对），关键章节齐全。
- `git diff --check`：通过。
- 本轮不修改网页运行逻辑、离线冻结文件或历史未跟踪脚本/截图。

## 发布结果

- 已提交并推送 `main`：`5f383a31b9831728490999371eeea6a81bf57ec5`（`docs: add complete online development guide`）。
- `git ls-remote origin refs/heads/main` 与本地提交一致，证明远端主分支已接收提交。
- GitHub 公共 API 在校验时返回匿名限流，无法读取 deployment API；改用公开资源交叉确认：`https://not1a-del.github.io/Moyun/` 返回 HTTP 200，`raw.githubusercontent.com/Not1a-del/Moyun/main/docs/ONLINE_VERSION_COMPLETE_DEVELOPER_GUIDE.md` 返回 HTTP 200 且包含文档标题。
- Pages 页面无需为纯文档改动重新生成应用资产；网页构建已在提交前通过。本次未改动网页运行逻辑。
