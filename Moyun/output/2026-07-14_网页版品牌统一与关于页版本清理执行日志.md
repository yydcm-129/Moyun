# 网页版品牌统一与关于页版本清理执行日志

日期：2026-07-14
状态：执行中

## 范围

- 唯一可写源：`source/moyun.single.html`，由 Pages 仓构建发布。
- 清除宿主界面可见版本号，并删除“关于”页的静态版本历史区块。
- 将宿主界面的“墨韵·造梦”（含带空格写法）统一为 `Moyun`。
- 复用现有 `assets/favicon/moyun.svg` 作为顶栏、侧栏与关于卡片的品牌图标。

## 保护项

- 不改冻结的离线单文件区、白鸟 JSON、交付包和旧测试资产。
- 不改用户小说数据、MOD/技能包的版本字段或内部 schema 的 `version` 字段。
- 不回滚此前已发布的连接中心、生成停止、弹窗和公告功能。

## 执行记录

- 已确认发布仓为 `单文件版/output/deploy-staging/`，当前 `main` 与远端一致；现存未跟踪测试/日志产物保持不动。
- 已定位品牌、宿主版本标识和“关于”页版本历史静态区块，待补丁与构建验收。
- 已替换宿主可见品牌、移除主页宿主版本标识，并整段删除“关于”页版本历史；未触及 MOD/用户数据版本字段。
- 首次真实桌面打开确认标题、首页和侧栏显示 `Moyun`；发现浏览器 favicon 请求 404，现将现有 SVG 声明为站点 favicon 后重建复验。
- `npm run build` 通过；`node --check assets/js/moyun.js` 通过；`git diff --check` 通过。
- 使用 Microsoft Edge 实测：桌面 `1440×960`、手机 `390×844`。两端标题均为 `Moyun`，旧品牌与宿主 `v2.4.1` 均未出现在页面文本；“关于”页 `details` 历史卡数量为 0、无“版本历史”文本；全部 `moyun.svg` 图像加载成功；横向宽度分别为 `1440/1440`、`390/390`；浏览器控制台为 0 error。
- 验收截图：`output/playwright/web-branding-desktop-1440-about.png`、`output/playwright/web-branding-mobile-about.png`；另保留主页截图 `web-branding-desktop-home.png`、`web-branding-mobile-home.png`。
- 发布前静态扫描确认生成的 `index.html` 与 `assets/js/moyun.js` 不含旧品牌、宿主 `v2.4.1` 或“版本历史”文本。
- 用户复核指出小尺寸手写字体下的 `Moyun` 辨识度不足；改用常规无衬线粗体，并同步调整加载页文字品牌。SVG 图标保持不变，待重新构建与桌面/手机复验。
- 二次 `npm run build`、`node --check assets/js/moyun.js` 与 `git diff --check` 均通过。真实 Edge 截图确认桌面 `1440×960`、手机 `390×844` 的 SVG 图标与常规粗体 `Moyun` 清晰可读，未产生新控制台错误。截图：`output/playwright/web-branding-readable-desktop.png`、`output/playwright/web-branding-readable-mobile.png`。
- 用户否决常规粗体方案后，按生成的开屏同气质概念图进行品牌字标重设：图标维持现有 SVG，文字统一为高对比衬线栈（Georgia / Times / Noto Serif），6px 图文间距、0 字距、浅色纸面和深墨开屏各自适配的对比度与轻投影；待构建和视觉验收。
- 设计概念：本轮通过内置 Image Generation 生成仅供实现参照的内联预览，采用“深墨开屏 + 暖白纸面、方形现有 SVG + 清晰编辑体衬线字标”的方向；未将生成图片作为产品资产，继续使用项目既有 SVG。
- 构建、JS 语法、差异检查和生成文件静态扫描均通过。Edge 实测桌面 `1440×960`、手机 `390×844`：字标字体均为 `Georgia / Times / Noto Serif`，SVG 成功加载，横向宽度精确等于视口，旧品牌/宿主版本/版本历史均为 0。真实设置页打开并切换“关于”通过。视觉验收截图：`web-branding-editorial-desktop.png`、`web-branding-editorial-mobile.png`、`web-branding-editorial-mobile-about.png`；开屏回放：`web-branding-editorial-loader-replay.png`。
- 已发布：提交 `807e7f4` 已推送至 `Not1a-del/Moyun` 的 `main`。Pages 线上 HTML 和 Edge 实测均加载 CSS `d564d41b9f2b`；桌面 `1440×960`、手机 `390×844` 各 0 console error、无横向溢出，字体、SVG、旧品牌/宿主版本/版本历史断言全部通过。线上截图：`online-branding-editorial-desktop.png`、`online-branding-editorial-mobile-clear.png`。
