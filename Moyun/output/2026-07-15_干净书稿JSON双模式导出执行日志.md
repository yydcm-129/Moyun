# 干净书稿 JSON 双模式导出执行日志

日期：2026-07-15
状态：已发布并完成 Pages 核验

## 用户要求

- 将网页版 JSON 导出改为两种干净模式：
  - 完整导出：世界观与正文；
  - 仅导出正文：正文且包含章节名。
- CoT、原生思考、工具记录、预设、流水线、文风、MOD、插件私有数据、图片及其他配置均不得导出。
- 本地测试后展示实际导出样本，再由用户确认上线。

## 变更边界

- 仅修改 Pages 仓 `source/moyun.single.html`、由其构建的静态产物与本日志。
- 不修改冻结离线区，不修改连接中心、生成逻辑、MOD 运行时或现有存档。

## 实施方案

1. 导出数据改为严格白名单，不再克隆整个 `book` 对象。
2. 两种模式都只保留导出元数据、书名、章节 `title/content`；完整模式额外保留 `novel.worldView`。
3. 正文走既有 `cleanNarrativeChapterContent`；世界观只移除明确 CoT / thinking 标签，不改写正常设定文字。
4. 用带有 CoT、预设、MOD 私有资料和模板内容的测试书走真实下载，检查字段、文本净化和导入兼容。

## 本地验收结果

- 构建通过：`npm run build`，生成 JS 指纹 `d74cb9a61112`；`node --check assets/js/moyun.js` 与 `git diff --check` 通过。
- Edge Playwright 导入了含 CoT、原生思考、工具时间线、章节历史稿、流水线、预设、文风、MOD 规则与插件私有工具结果的测试书，并分别走完“完整导出”“仅导出正文”的真实下载。
- 完整样本只含 `_type`、`_exportVersion`、`_exportMode`、`_exportDate`、`novel.title`、`novel.worldView` 与每章 `title/content`。
- 正文样本只含同一组元数据、`novel.title` 与每章 `title/content`，不含 `worldView`。
- 禁用字段和测试中的敏感文字扫描结果为零：`cotThinking`、`nativeThinking`、`toolTimeline`、`versions`、`promptPipeline`、`presets`、`writingStyles`、`modPacks`、`modPrivateData`、CoT、摘要、提示词资产均未出现在两个样本中。
- 已把完整样本重新导入网页，成功创建新书并显示 2 章、净化后的世界观；浏览器控制台无错误（仅有原有 Tailwind CDN 开发提示）。
- 手机视口实测：导出确认框在窄屏下完整显示两种模式和三条影响说明，按钮未横向溢出或被底部区域遮挡；截图为 `output/playwright/2026-07-15-clean-json-export-mobile-confirm.png`，仅供验收、不纳入提交。
- 本地样本：`output/playwright/clean-json-export-complete.json`、`output/playwright/clean-json-export-content-only.json`；仅供验收，暂不纳入提交。

## 发布记录

- 已提交并推送：`dd7db23 feat: export clean book JSON only`。
- Pages 已完成部署核验：页面引用 JS 指纹 `d74cb9a61112`；新资产包含 `moyun_clean_book_export` 与“仅导出正文”，不再包含旧的“包含图片 / 完整书籍结构”导出分支。
